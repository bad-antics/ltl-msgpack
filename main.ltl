// ltl-msgpack/main.ltl
//
// MessagePack codec with ext-types and streaming support.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-msgpack v0.1.0")
    io::println("MessagePack codec with ext-types and streaming support.")
    0
}
