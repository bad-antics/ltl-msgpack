// examples/demo.ltl — runnable demo for ltl_msgpack

import std::io
use ltl_msgpack as lib

fn main() {
    io::println("ltl_msgpack demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}